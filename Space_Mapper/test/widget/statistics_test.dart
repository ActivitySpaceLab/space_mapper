import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('statistics tests are temporarily disabled',
      (WidgetTester tester) async {
    expect(true, isTrue);
  }, skip: true);
}
